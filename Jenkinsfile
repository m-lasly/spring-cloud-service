pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(branch: 'master', url: 'https://github.com/m-lasly/spring-cloud-service.git', changelog: true, poll: true)
      }
    }
  }
}